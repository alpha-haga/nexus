/**
 * bridal モジュール
 *
 * 冠婚業務機能
 *
 * ルール:
 * - 他のモジュールへの直接 import は禁止
 * - identity / group への直接更新は禁止
 * - gojo への更新は禁止（参照のみ）
 * - @/services 経由でAPIを呼び出す
 * - @/modules/core のみ import 可能
 *
 * 依存:
 * - @/modules/core
 */

// コンポーネントをエクスポート
// export { BridalCaseForm } from './components/BridalCaseForm';
// export { BridalCaseList } from './components/BridalCaseList';
// export { VenueCalendar } from './components/VenueCalendar';
