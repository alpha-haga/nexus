/**
 * point モジュール
 *
 * ポイント管理機能
 *
 * ルール:
 * - 他のモジュールへの直接 import は禁止
 * - identity / group への直接更新は禁止
 * - @/services 経由でAPIを呼び出す
 * - @/modules/core のみ import 可能
 *
 * 依存:
 * - @/modules/core
 */

// コンポーネントをエクスポート
// export { PointBalance } from './components/PointBalance';
// export { PointHistory } from './components/PointHistory';
// export { PointTransfer } from './components/PointTransfer';
